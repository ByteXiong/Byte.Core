using Byte.Core.Api.Common;
using Byte.Core.SqlSugar;
using Microsoft.AspNetCore.Mvc;

namespace Byte.Core.Api.Controllers
{    /// <summary>
     /// 代码生成器
     /// </summary>
    [Route("api/[controller]/[action]")]
    public class CodeController : BaseApiController
    {

        //[HttpGet]
        //public List<DbTable> GetDBCore()
        //{
        //    var dbContext = ServiceLocator.Resolve<IDbContextCore>();
        //    if (dbContext == null)
        //        throw new Exception("未能获取到数据库上下文，请先注册数据库上下文。");
        //    var entity = dbContext.GetDatabaseTableList() as List<DbTable>;
        //    return entity;

        //}
 
    }
}
